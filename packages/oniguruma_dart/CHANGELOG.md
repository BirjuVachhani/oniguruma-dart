## 1.0.1

- Docs: add "Advanced features" and "Examples" sections to the README, with
  runnable examples for possessive quantifiers, atomic groups, subroutine calls
  and recursion (`\g<name>` / `\g<0>`), conditionals, variable-length look-behind,
  `\K` / `\R` / `\X`, character-class set operations, POSIX classes, Unicode
  properties, and inline modifiers.
- Docs: copyediting and formatting cleanup across the package documentation.

## 1.0.0

Initial release.
