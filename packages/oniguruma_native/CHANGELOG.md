## 1.0.0

Initial release — Oniguruma 6.9.10 regex bindings for Dart on **every
platform**, behind one vscode-oniguruma-compatible API (`OnigScanner`,
`OnigString`, `OnigMatch`) backed by the same engine everywhere.

- **IO** (Android / iOS / macOS / Linux / Windows / server): the native
  Oniguruma C library, wrapped in a thin C shim (`src/oniguruma_shim.c`) and
  called via `dart:ffi`.
- **Web** (dart2js and dart2wasm): the *same* Oniguruma + shim compiled to a
  self-contained wasm32-wasi module and driven over `dart:js_interop`, so
  results are byte-identical to the native backend. `isOnigurumaSupported` is
  `true` on web too.
- The right backend is chosen automatically by a conditional import, so
  consumers write one `import` and never deal with platform specifics.
- Call `await loadWasm()` once at startup before constructing a scanner. On web
  it loads the WebAssembly module (instantiation is asynchronous); on IO it is a
  no-op, so cross-platform startup code can call it unconditionally. After it
  resolves, every call is synchronous everywhere.
- The wasm module is **embedded in the package** (base64), so web is zero-setup.
  To trim your bundle, pass your own module to `loadWasm(bytes: ...)` or
  `loadWasm(url: ...)`.
- vscode-oniguruma-compatible multi-pattern scanning: `OnigScanner.findNextMatch`
  runs the whole scan loop inside the engine (in C on IO, in wasm on web), so
  there is exactly one boundary crossing per query.
- `OnigScanner.scanCount` counts every non-overlapping match across an input in a
  single crossing (no per-match allocation).
- UTF-16LE encoding, so match offsets line up 1:1 with Dart `String` indices with
  no remapping.
- Native library provided by a Dart build hook (`hook/build.dart`): bundles a
  SHA-256-verified prebuilt for the target when one ships under `prebuilt/`,
  otherwise downloads the pinned Oniguruma source (SHA-256 verified) and compiles
  it with the shim. Opt into the source path with the `oniguruma.from_source`
  user-define.
- Prebuilt native binaries for macOS, iOS (device + simulator), Linux, Android,
  and Windows, plus the wasm module; regenerated by the `prebuild-oniguruma`
  GitHub Actions workflow.

For a pure-Dart alternative that also runs on the web (and is faster for bulk
find-all-matches, with a lighter web bundle), see the sibling
[`oniguruma_dart`](../oniguruma_dart) package.
